name: Continuous Threat Modeling

# ── Triggers ──────────────────────────────────────────────────────────────────
# Runs on every push/PR that touches security-relevant files.
# workflow_dispatch lets you trigger it manually from the Actions tab.
on:
  push:
    branches: ["main"]
    paths:
      - "threagile/**.yaml"
      - "docker-compose.yml"
      - "nginx/nginx.conf"
      - "api/src/routes/**"
      - "api/src/middleware/**"
  pull_request:
    paths:
      - "threagile/**.yaml"
      - "docker-compose.yml"
      - "nginx/nginx.conf"
      - "api/src/routes/**"
      - "api/src/middleware/**"
  workflow_dispatch:

# ── Permissions ───────────────────────────────────────────────────────────────
permissions:
  contents: write        # commit updated baseline on main
  issues: write          # auto-create tickets for critical risks
  pull-requests: write   # post risk-diff comment on PRs
  security-events: write # upload risk + model-hygiene SARIF to code scanning

jobs:
  threat-model-analysis:
    name: "Threagile Analysis + Risk Diff"
    runs-on: ubuntu-latest

    steps:
      # ── 1. Checkout ──────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      # ── 2. Baseline ──────────────────────────────────────────────────────────
      # Snapshot the committed risks.json before overwriting it with new output.
      - name: Save baseline risks
        id: baseline
        run: |
          mkdir -p threagile/output
          if [ -f threagile/output/risks.json ]; then
            cp threagile/output/risks.json threagile/output/risks-previous.json
            echo "has_baseline=true" >> "$GITHUB_OUTPUT"
          else
            echo "[]" > threagile/output/risks-previous.json
            echo "has_baseline=false" >> "$GITHUB_OUTPUT"
          fi

      # ── 3. Analysis ──────────────────────────────────────────────────────────
      # Uses better-threagile (the fork) because this model uses the `includes:`
      # directive to split the model across multiple YAML files — a feature not
      # present in the upstream threagile/threagile Docker image.
      - name: Checkout better-threagile
        uses: actions/checkout@v4
        with:
          repository: Luv1881/better-threagile
          path: better-threagile

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: better-threagile/go.mod
          cache-dependency-path: better-threagile/go.sum

      # Graphviz provides `dot`, needed to render the data-flow / data-asset
      # diagrams that get embedded in the PDF report. Without it the PDF step
      # fails and aborts the whole job (skipping the issue-sync below).
      - name: Install Graphviz
        run: sudo apt-get update && sudo apt-get install -y graphviz

      - name: Build better-threagile binary
        run: |
          cd better-threagile
          go build -o bin/threagile ./cmd/threagile/

      - name: Run Threagile analysis
        run: |
          mkdir -p threagile/output
          better-threagile/bin/threagile analyze-model \
            --model      threagile/threagile.yaml \
            --output     threagile/output \
            --app-dir    better-threagile \
            --background report/template/background.pdf \
            --ignore-orphaned-risk-tracking

      # ── 3b. Code-scanning SARIF (risks + model hygiene) ──────────────────────
      # analyze-model already wrote risks.sarif; also produce a model-hygiene
      # SARIF from `lint` (stable rule IDs + file:line) and upload both so they
      # show up under the repo's Security → Code scanning tab. continue-on-error
      # so a repo without code scanning enabled still completes the pipeline.
      - name: Generate model-hygiene SARIF (lint)
        run: |
          better-threagile/bin/threagile lint \
            --model threagile/threagile.yaml \
            --ignore-orphaned-risk-tracking \
            --format sarif > threagile/output/lint.sarif

      - name: Upload risk SARIF to code scanning
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: threagile/output/risks.sarif
          category: threagile-risks

      - name: Upload model-hygiene SARIF to code scanning
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: threagile/output/lint.sarif
          category: threagile-lint

      # ── 4. Diff ──────────────────────────────────────────────────────────────
      - name: Diff risk output
        id: diff
        run: |
          python3 scripts/threat-model-diff.py \
            --previous threagile/output/risks-previous.json \
            --current  threagile/output/risks.json \
            --model    threagile/risk_tracking.yaml \
            --output   threagile/output/diff-report.json

      # ── 5. PR Comment ─────────────────────────────────────────────────────────
      - name: Comment on PR with risk diff
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs   = require('fs');
            const path = require('path').join(process.env.GITHUB_WORKSPACE, 'threagile/output/diff-report.json');
            const MARKER = '<!-- threat-model-bot -->';
            const runUrl = process.env.GITHUB_SERVER_URL + '/' + process.env.GITHUB_REPOSITORY + '/actions/runs/' + process.env.GITHUB_RUN_ID;
            if (!fs.existsSync(path)) {
              const errBody = MARKER + '\n## Threat Model Analysis\n\n' +
                '> Analysis failed - Threagile could not parse the model or the diff step did not complete.\n\n' +
                'Check the [workflow logs](' + runUrl + ') for details. Common causes:\n' +
                '- Invalid YAML indentation in one of the `threagile/*.yaml` files\n' +
                '- Unknown field value (run `docker run --rm threagile/threagile -list-types` locally)\n';
              const { data: ec } = await github.rest.issues.listComments({ owner: context.repo.owner, repo: context.repo.repo, issue_number: context.issue.number });
              const pe = ec.find(c => c.body && c.body.includes(MARKER));
              if (pe) { await github.rest.issues.updateComment({ owner: context.repo.owner, repo: context.repo.repo, comment_id: pe.id, body: errBody }); }
              else    { await github.rest.issues.createComment({ owner: context.repo.owner, repo: context.repo.repo, issue_number: context.issue.number, body: errBody }); }
              return;
            }

            const diff   = JSON.parse(fs.readFileSync(path, 'utf8'));
            const icon   = s => ({ critical:'🔴', elevated:'🟠', high:'🟡', medium:'🔵', low:'⚪' }[s] || '⚪');

            let body = `${MARKER}\n## 🔒 Threat Model Analysis\n\n`;
            body += `| | |\n|---|---|\n`;
            body += `| **Total risks (current)** | ${diff.total_current} |\n`;
            body += `| **New risks introduced**  | ${diff.new_risks.length} |\n`;
            body += `| **Risks resolved**        | ${diff.removed_risks.length} |\n`;
            body += `| **Net delta**             | ${diff.delta >= 0 ? '+' : ''}${diff.delta} |\n\n`;

            if (diff.new_risks.length > 0) {
              body += `### ⚠️ New Risks Introduced\n\n`;
              for (const r of diff.new_risks) {
                const sev   = (r.severity || 'unknown').toLowerCase();
                const asset = r.most_relevant_technical_asset || 'N/A';
                body += `${icon(sev)} **${r.title || r.synthetic_id}**  \`${sev}\`  — _${asset}_\n`;
                if (r.description) {
                  body += `> ${r.description.slice(0, 150).replace(/\n/g, ' ')}…\n`;
                }
                body += `> 💡 ${(r.mitigation || 'See full report.').slice(0, 130).replace(/\n/g, ' ')}\n\n`;
              }
            }

            if (diff.removed_risks.length > 0) {
              body += `### ✅ Risks Resolved\n\n`;
              for (const r of diff.removed_risks) {
                body += `- ~~${r.title || r.synthetic_id}~~\n`;
              }
              body += '\n';
            }

            if (diff.new_risks.length === 0 && diff.removed_risks.length === 0) {
              body += `### ✅ No risk changes\nThis PR does not introduce or resolve any threat model risks.\n`;
            }

            body += `\n---\n📎 [Full report, DFD and risk register in artifacts](${runUrl})`;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const prev = comments.find(c => c.body && c.body.includes(MARKER));
            if (prev) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner, repo: context.repo.repo,
                comment_id: prev.id, body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: context.issue.number, body,
              });
            }

      # ── 5b. Aggregate findings across the fork's cross-cutting rule packs ─────
      # The default analysis is STRIDE only, so findings from the fork-added
      # cloud-native / supply-chain / ai-ml packs never became tickets. Re-run
      # the model through each pack (each one supersets STRIDE) and merge the
      # results, de-duplicated by synthetic id, into all-risks.json — the
      # complete set the issue sync should track.
      - name: Aggregate findings across rule packs (for issue sync)
        if: >
          github.event_name == 'workflow_dispatch' ||
          (github.event_name == 'push' && github.ref == 'refs/heads/main')
        run: |
          for pack in cloud-native supply-chain ai-ml; do
            mkdir -p "threagile/output/packs/$pack"
            better-threagile/bin/threagile analyze-model \
              --model      threagile/threagile.yaml \
              --output     "threagile/output/packs/$pack" \
              --app-dir    better-threagile \
              --rule-pack  "$pack" \
              --ignore-orphaned-risk-tracking \
              --skip-report-pdf --skip-report-adoc \
              --skip-data-flow-diagram --skip-data-asset-diagram \
              --skip-risks-excel --skip-tags-excel
          done
          python3 scripts/merge-risks.py \
            threagile/output/all-risks.json \
            threagile/output/risks.json \
            threagile/output/packs/cloud-native/risks.json \
            threagile/output/packs/supply-chain/risks.json \
            threagile/output/packs/ai-ml/risks.json

      # ── 6. Full sync: create / close / reopen GitHub Issues ──────────────────
      # Syncs issues on push to main and on manual workflow_dispatch runs.
      # Skipped on pull_request events (those only get the diff comment).
      - name: Sync GitHub Issues for threat model findings
        if: >
          github.event_name == 'workflow_dispatch' ||
          (github.event_name == 'push' && github.ref == 'refs/heads/main')
        run: |
          python3 scripts/create-tickets.py \
            --risks        threagile/output/all-risks.json \
            --assets       threagile/output/technical-assets.json \
            --min-severity medium \
            --repo         "${{ github.repository }}" \
            --token        "${{ secrets.GITHUB_TOKEN }}"

      # ── 7. Pipeline Gate ──────────────────────────────────────────────────────
      # Blocks the PR if critical/elevated risks have no risk_tracking entry.
      - name: "Gate: fail on untracked critical risks"
        if: github.event_name == 'pull_request' && steps.diff.outputs.has_untracked_critical == 'true'
        run: |
          echo "::error title=Untracked Critical Risks::This PR introduces critical or elevated risks with no risk_tracking entry in threagile/threagile.yaml."
          echo ""
          echo "Add an entry under risk_tracking: in threagile/risk_tracking.yaml for each new risk:"
          echo ""
          echo "  risk_tracking:"
          echo "    <risk-synthetic-id>:"
          echo "      status: accepted | in-progress | mitigated"
          echo "      justification: \"reason\""
          echo "      ticket: VAULT-XXX"
          echo "      date: $(date +%Y-%m-%d)"
          echo "      checked_by: your-name"
          echo ""
          echo "Risk IDs are in the PR comment above and in the risks.xlsx artifact."
          exit 1

      # ── 8. Update baseline on main ────────────────────────────────────────────
      - name: Commit updated baseline on main
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add \
            threagile/output/risks.json \
            threagile/output/risks.xlsx \
            threagile/output/stats.json \
            threagile/output/data-flow-diagram.png 2>/dev/null || true
          git diff --staged --quiet || \
            git commit -m "chore: update threat model baseline [skip ci]"
          git push

      # ── 9. Upload artifacts ───────────────────────────────────────────────────
      - name: Upload threat model artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: "threat-model-run-${{ github.run_number }}"
          retention-days: 90
          path: |
            threagile/output/report.pdf
            threagile/output/data-flow-diagram.png
            threagile/output/risks.xlsx
            threagile/output/risks.json
            threagile/output/diff-report.json
            threagile/output/stats.json
