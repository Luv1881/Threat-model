threagile_version: 1.0.0
title: Imported from draw.io
business_criticality: archive
tags_available:
    - review-drawio
    - stub-data-asset
    - caching-layer
    - merge-absent-from-diagram
data_assets:
    Imported Data (review and classify):
        id: data-imported-drawio
        description: Stub data asset generated by drawio import. Review confidentiality and PII fields.
        usage: business
        tags:
            - review-drawio
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    MinIO Bucket-data:
        id: db1-drawio-data
        description: Stub data asset generated for datastore "Object Storage" — replace with the real data it stores.
        usage: business
        tags:
            - review-drawio
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    Session Cache-data:
        id: cache1-drawio-data
        description: Stub data asset generated for datastore "Session Cache" — replace with the real data it stores.
        usage: business
        tags:
            - review-drawio
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    user1 → web1-payload:
        id: link-user1-to-web1-e1-drawio-payload
        description: Stub data asset generated for internet-inbound link "user1 → web1" — replace with the real data assets sent/received.
        usage: business
        tags:
            - review-drawio
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    web1 → api1-payload:
        id: link-web1-to-api1-e2-drawio-payload
        description: Stub data asset generated for internet-inbound link "web1 → api1" — replace with the real data assets sent/received.
        usage: business
        tags:
            - review-drawio
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
technical_assets:
    API Server:
        id: api1-drawio
        description: Imported from draw.io shape (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: process
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: web-server
        tags:
            - review-drawio
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            api1 → db1:
                target: db1-drawio
                description: Imported from draw.io flow (review protocol/auth)
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-drawio
                usage: business
                data_assets_received:
                    - db1-drawio-data
            redis:
                target: cache1-drawio
                description: Imported from draw.io flow (review protocol/auth)
                protocol: nosql-access-protocol-encrypted
                authentication: none
                authorization: none
                tags:
                    - review-drawio
                    - caching-layer
                usage: business
                data_assets_received:
                    - cache1-drawio-data
    Customer Browser:
        id: user1-drawio
        description: Imported from draw.io shape (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: external-entity
        usage: business
        used_as_client_by_human: true
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: client-system
        tags:
            - review-drawio
        internet: true
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            user1 → web1:
                target: web1-drawio
                description: Imported from draw.io flow (review protocol/auth)
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-drawio
                usage: business
                data_assets_sent:
                    - link-user1-to-web1-e1-drawio-payload
    MinIO Bucket:
        id: db1-drawio
        description: Confirmed by a human reviewer — this is the production MinIO object-storage bucket.
        type: datastore
        usage: business
        size: system
        technology: object-storage
        tags:
            - merge-conflict:technology
            - merge-conflict:encryption
        machine: virtual
        encryption: data-with-symmetric-shared-key
        confidentiality: confidential
        integrity: critical
        availability: critical
        data_assets_stored:
            - db1-drawio-data
    Session Cache:
        id: cache1-drawio
        description: Imported from draw.io shape (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: datastore
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: in-memory-key-value-database
        tags:
            - review-drawio
            - caching-layer
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        data_assets_stored:
            - cache1-drawio-data
    Web Frontend:
        id: web1-drawio
        description: Imported from draw.io shape (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: process
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: web-server
        tags:
            - review-drawio
        internet: true
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            web1 → api1:
                target: api1-drawio
                description: Imported from draw.io flow (review protocol/auth)
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-drawio
                usage: business
                data_assets_sent:
                    - link-web1-to-api1-e2-drawio-payload
trust_boundaries:
    Application VPC:
        id: boundary-boundary1-drawio
        description: Imported from draw.io boundary
        # TODO(review): trust boundary type inferred from name/geometry/rating — confirm the real boundary type
        type: network-virtual-lan
        tags:
            - review-drawio
        technical_assets_inside:
            - api1-drawio
            - cache1-drawio
            - db1-drawio
            - web1-drawio
