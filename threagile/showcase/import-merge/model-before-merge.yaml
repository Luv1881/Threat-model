# Threagile scaffold generated by `threagile import drawio` (deterministic, no AI).
# Source: threagile/imports/vaultnote.drawio.xml
# Generated: 4 technical asset(s), 1 trust boundary(ies), 3 communication link(s), 4 data asset(s).

# Legend: everything tagged review-drawio or carrying a TODO(review) comment
# was inferred/defaulted by the importer and needs human confirmation before
# this model is trusted for analysis. Re-run with --scaffold=false for plain output.
threagile_version: 1.0.0
title: Imported from draw.io
business_criticality: archive
tags_available:
    - review-drawio
    - stub-data-asset
data_assets:
    Imported Data (review and classify):
        id: data-imported-drawio
        description: Stub data asset generated by drawio import. Review confidentiality and PII fields.
        usage: business
        tags:
            - review-drawio
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    MinIO Bucket-data:
        id: db1-drawio-data
        description: Stub data asset generated for datastore "MinIO Bucket" — replace with the real data it stores.
        usage: business
        tags:
            - review-drawio
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    user1 → web1-payload:
        id: link-user1-to-web1-e1-drawio-payload
        description: Stub data asset generated for internet-inbound link "user1 → web1" — replace with the real data assets sent/received.
        usage: business
        tags:
            - review-drawio
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    web1 → api1-payload:
        id: link-web1-to-api1-e2-drawio-payload
        description: Stub data asset generated for internet-inbound link "web1 → api1" — replace with the real data assets sent/received.
        usage: business
        tags:
            - review-drawio
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
technical_assets:
    API Server:
        id: api1-drawio
        description: Imported from draw.io shape (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: process
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: web-server
        tags:
            - review-drawio
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            api1 → db1:
                target: db1-drawio
                description: Imported from draw.io flow (review protocol/auth)
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-drawio
                usage: business
                data_assets_received:
                    - db1-drawio-data
    Customer Browser:
        id: user1-drawio
        description: Imported from draw.io shape (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: external-entity
        usage: business
        used_as_client_by_human: true
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: client-system
        tags:
            - review-drawio
        internet: true
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            user1 → web1:
                target: web1-drawio
                description: Imported from draw.io flow (review protocol/auth)
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-drawio
                usage: business
                data_assets_sent:
                    - link-user1-to-web1-e1-drawio-payload
    MinIO Bucket:
        id: db1-drawio
        description: Imported from draw.io shape (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: datastore
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: file-server
        tags:
            - review-drawio
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        data_assets_stored:
            - db1-drawio-data
    Web Frontend:
        id: web1-drawio
        description: Imported from draw.io shape (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: process
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: web-server
        tags:
            - review-drawio
        internet: true
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            web1 → api1:
                target: api1-drawio
                description: Imported from draw.io flow (review protocol/auth)
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-drawio
                usage: business
                data_assets_sent:
                    - link-web1-to-api1-e2-drawio-payload
trust_boundaries:
    Application VPC:
        id: boundary-boundary1-drawio
        description: Imported from draw.io boundary
        # TODO(review): trust boundary type inferred from name/geometry/rating — confirm the real boundary type
        type: network-virtual-lan
        tags:
            - review-drawio
        technical_assets_inside:
            - api1-drawio
            - db1-drawio
            - web1-drawio
