# Threagile scaffold generated by `threagile import otm` (deterministic, no AI).
# Source: threagile/imports/vaultnote.otm.json
# Generated: 5 technical asset(s), 2 trust boundary(ies), 4 communication link(s), 4 data asset(s).

# Legend: everything tagged review-otm or carrying a TODO(review) comment
# was inferred/defaulted by the importer and needs human confirmation before
# this model is trusted for analysis. Re-run with --scaffold=false for plain output.
threagile_version: 1.0.0
title: VaultNote
business_criticality: archive
tags_available:
    - caching-layer
    - primary-datastore
    - review-otm
    - stub-data-asset
data_assets:
    Browse-payload:
        id: flow-df-1-c-browser-c-web-otm-payload
        description: Stub data asset generated for internet-inbound link "Browse" — replace with the real data assets sent/received.
        usage: business
        tags:
            - review-otm
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    Encrypted Notes:
        id: asset-a-notes-otm
        usage: business
        tags:
            - review-otm
            - primary-datastore
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: strictly-confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: mission-critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: important
    Notes Database-data:
        id: comp-c-db-otm-data
        description: Stub data asset generated for datastore "Notes Database" — replace with the real data it stores.
        usage: business
        tags:
            - review-otm
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    Session Cache-data:
        id: comp-c-cache-otm-data
        description: Stub data asset generated for datastore "Session Cache" — replace with the real data it stores.
        usage: business
        tags:
            - review-otm
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
technical_assets:
    API Server:
        id: comp-c-api-otm
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: process
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: web-service-rest
        tags:
            - review-otm
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            Query notes:
                target: comp-c-db-otm
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-otm
                    - primary-datastore
                usage: business
                data_assets_received:
                    - comp-c-db-otm-data
            Session lookup:
                target: comp-c-cache-otm
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-otm
                usage: business
                data_assets_received:
                    - comp-c-cache-otm-data
    Customer Browser:
        id: comp-c-browser-otm
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: external-entity
        usage: business
        used_as_client_by_human: true
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: browser
        tags:
            - review-otm
        internet: true
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            Browse:
                target: comp-c-web-otm
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-otm
                usage: business
                data_assets_sent:
                    - flow-df-1-c-browser-c-web-otm-payload
    Notes Database:
        id: comp-c-db-otm
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: datastore
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: database
        tags:
            - review-otm
            - primary-datastore
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        data_assets_stored:
            - comp-c-db-otm-data
    Session Cache:
        id: comp-c-cache-otm
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: datastore
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: in-memory-key-value-database
        tags:
            - review-otm
            - caching-layer
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        data_assets_stored:
            - comp-c-cache-otm-data
    Web Frontend:
        id: comp-c-web-otm
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: process
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: web-application
        tags:
            - review-otm
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            API calls:
                target: comp-c-api-otm
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-otm
                usage: business
trust_boundaries:
    Application VPC:
        id: zone-tz-app-otm
        # TODO(review): trust boundary type inferred from name/geometry/rating — confirm the real boundary type
        type: network-virtual-lan
        tags:
            - review-otm
        technical_assets_inside:
            - comp-c-api-otm
            - comp-c-cache-otm
            - comp-c-db-otm
            - comp-c-web-otm
    Internet:
        id: zone-tz-internet-otm
        # TODO(review): trust boundary type inferred from name/geometry/rating — confirm the real boundary type
        type: network-on-prem
        tags:
            - review-otm
        technical_assets_inside:
            - comp-c-browser-otm
