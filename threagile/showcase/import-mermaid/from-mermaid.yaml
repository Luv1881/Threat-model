# Threagile scaffold generated by `threagile import mermaid` (deterministic, no AI).
# Source: threagile/imports/vaultnote.mmd
# Generated: 5 technical asset(s), 1 trust boundary(ies), 4 communication link(s), 5 data asset(s).

# Legend: everything tagged review-mermaid or carrying a TODO(review) comment
# was inferred/defaulted by the importer and needs human confirmation before
# this model is trusted for analysis. Re-run with --scaffold=false for plain output.
threagile_version: 1.0.0
title: Imported from Mermaid
business_criticality: archive
tags_available:
    - caching-layer
    - primary-datastore
    - review-mermaid
    - stub-data-asset
data_assets:
    HTTPS-payload:
        id: link-user-to-web-mermaid-payload
        description: Stub data asset generated for internet-inbound link "HTTPS" — replace with the real data assets sent/received.
        usage: business
        tags:
            - review-mermaid
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    HTTPS-payload (link-web-to-api-mermaid-payload):
        id: link-web-to-api-mermaid-payload
        description: Stub data asset generated for internet-inbound link "HTTPS" — replace with the real data assets sent/received.
        usage: business
        tags:
            - review-mermaid
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    Imported Data (review and classify):
        id: data-imported-mermaid
        description: Stub data asset generated by mermaid import. Review confidentiality and PII fields.
        usage: business
        tags:
            - review-mermaid
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    Notes Database-data:
        id: db-mermaid-data
        description: Stub data asset generated for datastore "Notes Database" — replace with the real data it stores.
        usage: business
        tags:
            - review-mermaid
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
    Session Cache-data:
        id: cache-mermaid-data
        description: Stub data asset generated for datastore "Session Cache" — replace with the real data it stores.
        usage: business
        tags:
            - review-mermaid
            - stub-data-asset
        quantity: very-few
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
technical_assets:
    API Server:
        id: api-mermaid
        description: Imported from mermaid flowchart node (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: process
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: web-server
        tags:
            - review-mermaid
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            Redis:
                target: cache-mermaid
                description: Imported from mermaid flow (review protocol/auth)
                protocol: nosql-access-protocol-encrypted
                authentication: none
                authorization: none
                tags:
                    - review-mermaid
                    - caching-layer
                usage: business
                data_assets_received:
                    - cache-mermaid-data
            SQL/TLS:
                target: db-mermaid
                description: Imported from mermaid flow (review protocol/auth)
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-mermaid
                usage: business
                data_assets_received:
                    - db-mermaid-data
    Customer Browser:
        id: user-mermaid
        description: Imported from mermaid flowchart node (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: external-entity
        usage: business
        used_as_client_by_human: true
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: client-system
        tags:
            - review-mermaid
        internet: true
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            HTTPS:
                target: web-mermaid
                description: Imported from mermaid flow (review protocol/auth)
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-mermaid
                usage: business
                data_assets_sent:
                    - link-user-to-web-mermaid-payload
    Notes Database:
        id: db-mermaid
        description: Imported from mermaid flowchart node (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: datastore
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: database
        tags:
            - review-mermaid
            - primary-datastore
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        data_assets_stored:
            - db-mermaid-data
    Session Cache:
        id: cache-mermaid
        description: Imported from mermaid flowchart node (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: datastore
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: in-memory-key-value-database
        tags:
            - review-mermaid
            - caching-layer
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        data_assets_stored:
            - cache-mermaid-data
    Web Frontend:
        id: web-mermaid
        description: Imported from mermaid flowchart node (review classification)
        # TODO(review): asset type inferred from diagram shape/label — confirm this matches the real asset (external-entity/process/datastore)
        type: process
        usage: business
        size: system
        # TODO(review): technology guessed from the shape name/type keywords — set the actual technology
        technology: web-server
        tags:
            - review-mermaid
        internet: true
        # TODO(review): machine defaulted to a conservative guess — confirm the actual deployment (physical/virtual/container/serverless)
        machine: virtual
        # TODO(review): encryption defaulted conservatively — set the actual encryption in use
        encryption: none
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        confidentiality: confidential
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        integrity: critical
        # TODO(review): CIA rating defaulted conservatively — confirm against the real data classification
        availability: critical
        communication_links:
            HTTPS:
                target: api-mermaid
                description: Imported from mermaid flow (review protocol/auth)
                protocol: https
                authentication: none
                authorization: none
                tags:
                    - review-mermaid
                usage: business
                data_assets_sent:
                    - link-web-to-api-mermaid-payload
trust_boundaries:
    Application VPC:
        id: boundary-application-vpc-mermaid
        description: Imported from mermaid subgraph
        # TODO(review): trust boundary type inferred from name/geometry/rating — confirm the real boundary type
        type: network-virtual-lan
        tags:
            - review-mermaid
        technical_assets_inside:
            - api-mermaid
            - cache-mermaid
            - db-mermaid
            - web-mermaid
